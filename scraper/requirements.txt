# Local scraper only — deliberately kept out of the Vercel-facing
# requirements.txt (Playwright needs a real browser binary and doesn't
# run in a serverless function anyway).
playwright==1.48.0
requests==2.31.0
beautifulsoup4==4.12.3
